packages:
  - packages/*
  - api
  - scripts
  - admin
  - web

# These three ship prebuilt native binaries that their install scripts unpack.
# wrangler cannot run without workerd, and the image pipeline cannot run
# without sharp.
allowBuilds:
  esbuild: true
  workerd: true
  sharp: true
